#include <stdio.h>

int main()
{
    int bloarf;

   bloarf = 22;

   printf("La valeur de bloarf est %d. \n", bloarf);
   printf("La valeur de bloarf plus 16 est %d. \n", bloarf+16);
   printf("La valeur de bloarf puissance deux est %d. \n", bloarf*bloarf);
    return 0;
}
